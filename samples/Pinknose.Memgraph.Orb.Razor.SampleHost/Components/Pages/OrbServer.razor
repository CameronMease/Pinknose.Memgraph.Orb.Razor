@page "/orb-server"
@rendermode InteractiveServer

@* Thin by design: this page and OrbWasm.razor must differ only by render mode, so every
   demo feature belongs in OrbDemoView. See OrbSamplePagesTests. *@

<PageTitle>Orb (Server)</PageTitle>

<h1>Orb (Server)</h1>

<OrbDemoView />
